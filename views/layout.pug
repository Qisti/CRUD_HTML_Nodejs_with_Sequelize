doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css')
    link(rel='stylesheet', href='https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css', integrity='sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm', crossorigin='anonymous')
    link(href='https://cdn.jsdelivr.net/npm/tailwindcss/dist/tailwind.min.css', rel='stylesheet')
    link(href='https://cdn.jsdelivr.net/npm/tailwindcss/dist/preflight.min.css', rel='stylesheet')
    link(href='https://cdn.jsdelivr.net/npm/tailwindcss/dist/utilities.min.css', rel='stylesheet')
    script(type='text/javascript').
      function confirm_delete() {
        return confirm('are you sure you want delete this student?');
      }
      function confirm_update() {
        return confirm('are you sure you want update this student ?');
      }
      function confirm_logout(){
        return confirm('Are you sure you want log out?');
      }
      function dateValitaion(){
        if (date >= now) {
        return alert('Tanggal yang dimasukkan salah');
      }
      }
      

  body.col-md-auto.p-4.flex.mb-4.my-4
      .rounded.overflow-hidden.justify-center.shadow-lg.w-full.h-full
        nav.flex.items-center.justify-between.bg-blue-dark.p-2
         .w-full.block.flex-grow(class='lg:flex lg:items-center lg:w-auto')
          .text-sm.mb-2(class='lg:flex-grow')
            a.block.mt-2.text-white.mr-4.ml-4(href='/students', class='lg:inline-block lg:mt-0 hover:text-blue-lightest')
              | Home
            a.block.mt-2.text-white.mr-4(href='/statistic/?year=2018', class='lg:inline-block lg:mt-0 hover:text-blue-lightest')
              | Statistic
            a.block.mt-2.text-white.mr-4(href='/students', class='lg:inline-block lg:mt-0 hover:text-blue-lightest')
              | Student List
            a.block.mt-2.text-white.mr-4(href='/input', class='lg:inline-block lg:mt-0 hover:text-blue-lightest')
              | Add Student
            a.block.mt-2.text-white.mr-4(href='/addUser', class='lg:inline-block lg:mt-0 hover:text-blue-lightest')
              | Add Admin
            a.block.mt-2.text-white.mr-4(href='/admin/list', class='lg:inline-block lg:mt-0 hover:text-blue-lightest')
              | Admin List
          .flex.justify-right
            a.block.mt-2.text-white.mr-4(class='lg:inline-block lg:mt-0 hover:text-blue-lightest', href='/login', method='GET', onClick='return confirm_logout()')
              | Logout
        block content